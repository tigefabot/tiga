version: 2
jobs:
  build:
    docker:
    - image: registry.gitlab.com/tigefa/ffmpeg:latest
    steps:
    - checkout
    - run:
        name: Requirements
        command: |
          apt-get update && apt-get dist-upgrade -yqq && apt-get install -yqq sudo lsb-release apt-transport-https ca-certificates software-properties-common wget curl netcat aria2 whois figlet git p7zip p7zip-full zip unzip
          apt-get install -yqq wget aria2 httpie ffmpeg speedtest-cli telnet lshw tree
    - run:
        name: Downloading
        command: |
         aria2c --check-certificate=false --min-split-size=100M --max-concurrent-downloads=16 --max-connection-per-server=16 "https://www.dropbox.com/s/dl/d7eeidrmrkadahk/The.Penthouse.E29-E30.201214.720p-NEXT.mp4"
    - run:
        name: Rendering
        command: |
          ulimit -c unlimited
          ffmpeg -i "The.Penthouse.E29-E30.201214.720p-NEXT.mp4" -threads 16 -c:v libx265 -c:a copy -metadata title="Penthouse Episode 14" -preset veryfast 14.mkv
          ls -hal
    - store_artifacts:
        path: 14.mkv
workflows:
  version: 2
  workflow:
    jobs:
    - build
